import SwiftUI

enum AppColor {
    static let accent = Color(red: 0.22, green: 0.64, blue: 0.56)
    static let fastingRing = Color(red: 0.38, green: 0.56, blue: 0.90)
    static let eatingRing = Color(red: 0.96, green: 0.65, blue: 0.27)
    static let background = Color(.systemBackground)
    static let secondaryBackground = Color(.secondarySystemBackground)
    static let destructive = Color.red
}
